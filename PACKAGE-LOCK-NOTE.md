# package-lock.json

`package-lock.json` is intentionally not rewritten with comments because it is machine-generated JSON and comments would make it invalid. Keep the repository's original `package-lock.json` unchanged so npm can use it for reproducible dependency installation.
